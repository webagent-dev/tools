import styled from 'styled-components'

export const  YoutubesContainer = styled.div`
            margin-top: 170px;
    max-width: 80%;
    flex: 3;
     background: linear-gradient( to top, rgba(0,0,0,0.2), rgba(0,0,0,0.2)), url(${props => props.bg});
    background-position: center;
    background-size: cover;
    background-repeat: no-repeat;
`